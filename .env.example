# 실제 값은 Git에 커밋하지 않는다. Cloudflare에서는 `wrangler secret put <NAME>` 로 등록한다.
# 교사 로그인 (wrangler.jsonc vars 기본값을 덮어쓰려면 secret 으로 등록)
TEACHER_ID=teacher
TEACHER_PASSWORD=
# GitHub Actions 배포용 (저장소 Secrets 에 등록)
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
